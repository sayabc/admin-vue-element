const getters = {
  sidebar: state => state.global.sidebar,
  permissionRouters: state => state.global.permissionRouters,
  enterSiteStyle: state => state.global.enterSiteStyle,
};

export default getters;
